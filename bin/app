#!/usr/bin/env php
<?php
require __DIR__.'/../vendor/autoload.php';

use App\Cli\Command\DownloadVideos;
use Symfony\Component\Console\Application;

putenv('PROJECT_ROOT='.dirname(__DIR__));

$application = (new Application())
    ->add(new DownloadVideos())
    ->getApplication()
    ->setDefaultCommand(DownloadVideos::COMMAND_NAME, true)
    ->run();
